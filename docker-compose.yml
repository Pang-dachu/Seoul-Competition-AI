# 도커 컴포즈의 버전
version: "3.5"

services:
  fastapi:
    container_name: fastapi
    ports:
      - "8000:8000"
    build:
      context: ./
      dockerfile: Dockerfile
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    restart: always
    volumes:
      - ./:/server
